let 이름: string = "kim";
let 나이: number = 50;

let 결혼유무: boolean = true;

// null과 undefined도 타입으로 지정할 수 있다.
let 널: null = null;

// 배열
let 회원들: string[] = ["kim", "park"];

// 객체
let 회원들1: { member1: string; member2: string } = {
  member1: "kim",
  member2: "park",
};

// 타입 지정을 굳이 할 필요는 없다. 
// 타입 스크립트는 알아서 타입 지정을 해준다. 
// 타입지정 문법이 생략이 가능하다. 

